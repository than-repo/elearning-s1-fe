# UI Component Conventions

This folder is for reusable, app-agnostic UI primitives only.

- Keep components small, accessible, and typed.
- Do not import feature code from this folder.
- Prefer Tailwind tokens from `src/styles/globals.css` over hardcoded colors.
- Add a component only when at least one real screen needs it.